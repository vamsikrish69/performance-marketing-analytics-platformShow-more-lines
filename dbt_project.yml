name: performance_marketing_analytics_platform
version: '1.0.0'
config-version: 2

profile: default

model-paths: ["models"]
analysis-paths: ["analyses"]
seed-paths: ["data/seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  performance_marketing_analytics_platform:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table

seeds:
  performance_marketing_analytics_platform:
    +quote_columns: false
